# VoisLab Environment Configuration Template
# Copy this file to .env.local and fill in your actual values
# .env.local is git-ignored and will not be committed

# AWS Configuration
VITE_AWS_REGION=us-west-2
VITE_ENVIRONMENT=dev

# Backend Resources
# Get these values from: infrastructure/outputs-dev.json
# Run: cd infrastructure && cat outputs-dev.json
VITE_DYNAMODB_TABLE_NAME=voislab-audio-metadata-dev
VITE_S3_MEDIA_BUCKET=voislab-media-dev-YOUR_ACCOUNT_ID
VITE_CLOUDFRONT_DOMAIN=YOUR_CLOUDFRONT_DOMAIN.cloudfront.net

# Analytics (Optional)
# Replace with your Google Analytics Measurement ID
VITE_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Feature Flags (Optional)
# Set to true to enable error reporting and performance monitoring
VITE_ERROR_REPORTING_ENABLED=false
VITE_PERFORMANCE_MONITORING_ENABLED=false

# AWS Credentials
# DO NOT set these here! Use AWS CLI credentials instead.
# Your credentials are stored securely in ~/.aws/credentials
# Run: aws configure
